# Local environment / secrets — never share
.env
.venv/
__pycache__/
*.pyc

# Build-time scripts, source dataset, intermediates, logs — not part of the shared app
irrelevant/

# Runtime-generated output (regenerated by the app; not source)
data/one_pagers/
data/knowledge.jsonl
data/*.log
data/chroma/

# Frontend dependencies / build output
frontend/node_modules/
frontend/dist/

Test Dataset/
